import 'package:flutter/material.dart';

class Constants {
  static const iconSize = 48.0;
  static const iconSpacing = 24.0;

  static const lightSwatch = Colors.deepOrange;
  static const darkSwatch = Colors.amber;
  static const fontFamily = 'OpenSans';
}